[project]
name = "imgconvert"
version = "0.1.0"
description = "A minimal GUI image format converter (SVG/JPG/PNG/WebP)."
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
include = ["imgconvert*"]
